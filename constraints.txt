# Fully pinned environment: what CI installs, and what the test suite was last
# green on. Install with it to reproduce the reference environment exactly:
#
#   python -m pip install -e ".[dev]" -c constraints.txt
#
# Generated on Python 3.11. It is *not* installable on 3.10 (websockets 17
# requires >=3.11); on 3.10 install from the ranges in pyproject.toml and let
# pip resolve an older websockets. CI encodes exactly that.
#
# Refresh deliberately (then run the tests):
#   python -m venv .venv-refresh && .venv-refresh/bin/pip install -e .
#   .venv-refresh/bin/pip freeze | grep -v "^machinelearningmachine" > constraints.txt
#
# pyproject.toml / requirements.txt define the supported ranges; this file
# removes the version drift that "works on my machine" bugs come from.
aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiosignal==1.4.0
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
attrs==26.1.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
fastapi==0.141.1
frozenlist==1.8.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.20
iniconfig==2.3.0
multidict==6.9.0
packaging==26.3
pluggy==1.6.0
propcache==0.5.4
pydantic==2.13.5
pydantic_core==2.46.5
Pygments==2.21.0
pytest==9.1.1
pytest-asyncio==1.4.0
requests==2.34.2
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.8.0
uvicorn==0.53.0
websockets==17.1
yarl==1.25.1
